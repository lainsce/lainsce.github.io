/*****************
*     CARDS     *
*****************/

.card {
  background-color: $card;
  border-radius: 8px;
  display: block;
  line-height: 1.5rem;
  transition: all 250ms;
  @include box-shadow(1, rgba($fg,0.2));

  &.card-stacked {
      display: flex;
      flex: 1;
      flex-direction: column;
      position: relative;
  }

  &.horizontal {
      border-radius: 0 8px 8px 0;
      margin-left: 0px;

      &.card-content {
          border-radius: 0 0 8px 8px;
          padding: 24px;
          box-shadow: none;

          & p {
              color: inherit;
              padding: 0;
          }
      }
  }

  &.card-alone {
    margin-right: 0;
    vertical-align: middle;
  }
}

.circle {
  background-color: $badge;
  border: 1px solid $badge;
  border-radius: 50%;
  height: 70px;
  margin: 10px 5px -25px -5px;
  max-height: 100%;
  max-width: 100%;
  object-fit: cover;
  overflow-y: hidden;
  width: 70px;
}

.book {
  background-color: $badge;
  border: 1px solid $badge;
  border-radius: 8px;
  bottom: -100%;
  height: 90px;
  left: -100%;
  margin: 10px 5px -25px -5px;
  object-fit: cover;
  overflow-y: hidden;
  right: -100%;
  top: -100%;
  width: 70px;
}

.card-title {
  @include font($size: 2.25rem, $color: $fg, $weight: 400);
  margin-bottom: 2rem;
  margin-top: 2rem;

  & p.card-title-text {
    margin-bottom: 0px;
    margin-top: 0px;
    color: $button-info;
    text-align: left;
  }

  & i {
    padding-right: 5px;
    margin-left: -5px;
    display: inline;
  }
}

.card-subtitle {
  @include font($size: 1.5rem, $color: $fg, $weight: 400);
  margin-bottom: 1rem;
  margin-top: 1rem;

  & p {
    margin-bottom: 0px;
    margin-top: 0px;
  }
}

.card-subsubtitle {
  @include font($size: 1.25rem, $color: $fg, $weight: 400);
  margin-bottom: 10px;
  margin-top: 10px;

  & p {
    margin-bottom: 0px;
    margin-top: 0px;
  }
}

.card-text {
  @include font($size: 1rem, $color: $fg, $weight: 400);

  & p {
    margin-bottom: 10px;
    margin-top: 10px;
  }

  &.card-text-music {
    margin-left: 78px;
  }
}

.card-title-night {
  @include font($size: 2.25rem, $color: $bg, $weight: 400);
  margin-bottom: 2rem;
  margin-top: 2rem;

  & p {
    margin-bottom: 0px;
    margin-top: 0px;
  }
}

.card-subtitle-night {
  @include font($size: 1.5rem, $color: $bg, $weight: 400);
  margin-bottom: 1rem;
  margin-top: 1rem;

  & p {
    margin-bottom: 0px;
    margin-top: 0px;
  }
}

.card-subsubtitle-night {
  @include font($size: 1.25rem, $color: $bg, $weight: 400);
  margin-bottom: 10px;
  margin-top: 10px;

  & p {
    margin-bottom: 0px;
    margin-top: 0px;
  }
}

.card-text-night {
  @include font($size: 1rem, $color: $bg, $weight: 400);

  & p {
    margin-bottom: 10px;
    margin-top: 10px;
  }

  &.card-text-music {
    margin-left: 78px;
  }
}

.card-image {
  border-radius: 8px 8px 0 0;
  bottom: 0;
  display: block;
  border-bottom: 1px solid $card-action;
  left: 0;
  padding: 1.8rem 0;
  position: relative;
  right: 0;
  text-align: center;
  top: 0;

  &.card-flair-aesop {
    border-top: 8px solid $aesop;
    background-color: rgba($aesop,0.3);
  }

  &.card-flair-notejot {
    border-top: 8px solid $notejot;
    background-color: rgba($notejot,0.3);
  }

  &.card-flair-quilter {
    border-top: 8px solid $quilter;
    background-color: rgba($quilter,0.3);
  }

  &.card-flair-coin {
    border-top: 8px solid $coin;
    background-color: rgba($coin,0.3);
  }

  &.card-flair-palaura {
    border-top: 8px solid $palaura;
    background-color: rgba($palaura,0.3);
  }

  &.card-flair-yishu {
    border-top: 8px solid $yishu;
    background-color: rgba($yishu,0.3);
  }

  &.card-flair-beemy {
    border-top: 8px solid $beemy;
    background-color: rgba($beemy,0.3);
  }

  &.card-flair-timetable {
    border-top: 8px solid $timetable;
    background-color: rgba($timetable,0.3);
  }

  &.card-flair-rccar {
    border-top: 8px solid $rccar;
    background-color: rgba($rccar,0.3);
  }
}

.card-banner {
  border-radius: 8px 8px 0 0;
  bottom: 0;
  display: block;
  left: 0;
  padding: .5rem;
  position: relative;
  right: 0;
  text-align: center;
  top: 0;

  &.horizontal {
      border-radius: .3rem 0 0 .3rem;
      max-width: 40%;
      text-align: center;
  }

  &.card-flair-hack-17 {
    border-left: 8px solid $hack-2017;
    background-color: rgba($hack-2017,0.3);
  }

  &.card-flair-hack-18 {
    border-left: 8px solid $hack-2018;
    background-color: rgba($hack-2018,0.3);
  }
}

.card-image-big {
  border-radius: 8px 8px 0 0;
  border-bottom: 1px solid $card-action;
  bottom: 0;
  display: block;
  left: 0;
  padding: 1rem 0;
  position: relative;
  right: 0;
  text-align: center;
  top: 0;

  &.card-flair-aesop {
    border-top: 8px solid $aesop;
    background-color: rgba($aesop,0.3);
  }

  &.card-flair-notejot {
    border-top: 8px solid $notejot;
    background-color: rgba($notejot,0.3);
  }

  &.card-flair-quilter {
    border-top: 8px solid $quilter;
    background-color: rgba($quilter,0.3);
  }

  &.card-flair-coin {
    border-top: 8px solid $coin;
    background-color: rgba($coin,0.3);
  }

  &.card-flair-palaura {
    border-top: 8px solid $palaura;
    background-color: rgba($palaura,0.3);
  }

  &.card-flair-yishu {
    border-top: 8px solid $yishu;
    background-color: rgba($yishu,0.3);
  }

  &.card-flair-beemy {
    border-top: 8px solid $beemy;
    background-color: rgba($beemy,0.3);
  }

  &.card-flair-timetable {
    border-top: 8px solid $timetable;
    background-color: rgba($timetable,0.3);
  }

  &.card-flair-rccar {
    border-top: 8px solid $rccar;
    background-color: rgba($rccar,0.3);
  }
}

.card-inside-img {
  bottom: 0;
  display: inline;
  left: 0;
  margin: 0 auto;
  max-width: 60%;
  padding: 1rem;
  right: 0;
  top: 0;
}

.card-inside-banner {
  bottom: 0;
  display: inline;
  left: 0;
  margin: 0 auto;
  max-width: 70%;
  padding: 0;
  right: 0;
  top: 0;
}

.card-action {
  @include font($size: .9rem, $weight: 500);
  border-top: 1px solid $card-action;
  background-color: $card-body;
  border-radius: 0 0 8px 8px;
  margin: 0;
  padding: 0.8rem 1.5rem;
  position: relative;
  text-align: left;

  & a {
      border: 2px solid transparent;
      border-radius: .2rem;
      margin: 0;
      padding: 8px;

    &:hover {
      border-radius: .2rem;
      padding: 8px;
    }

    &:active {
      color: $bg;
    }
  }

  & .fas,.fab {
    margin-right: 12px;
  }

  &.horizontal {
      border-radius: 0 0 8px 0;
  }
}

.card-body {
  padding: 1.5rem;
  background-color: $card-body;
}

.card-chip {
  text-align: justify;
}

.card-skillbar {
  background-color: $card-skillbar;
  border-radius: 1rem;
  color: $bg;
  line-height: 2rem;
  padding-left: 20px;
  text-align: left;
}

.card-stars {
  color: $card-stars;
}
